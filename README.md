# Storiny Discovery

A standalone Rust service that handles embed (iframe) generation for Storiny.

## Providers & embeds

- Providers are defined in `providers/providers.json`.
- Embeds are rendered using Sailfish templates from `templates/`
